///sets the player's mask depending on gravity flip mode

if (global.grav == 1)
//    mask_index = sprPlayerMask;
mask_index = spr_7735box
else
    //mask_index = sprPlayerMaskFlip;
    mask_index = spr_7735box
